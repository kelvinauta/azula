{
  "name": "kleb_azula",
  "scripts": {
    "dev": "bun run --watch src/index.js",
    "start": "bun run src/index.js",
    "test": "bun run test",
    "test-jest": "jest ./src/tests/factory.test.js",
    "reset-db": "rm -rf .volumes && docker compose down && docker compose up -d",
    "docker-dev": "docker compose down && docker compose up -d && docker exec -it workspace_azula /bin/bash",
    "docker-shell-dev": "bun install && ./cli/dev.sh"
  },
  "dependencies": {
    "@faker-js/faker": "^9.2.0",
    "dotenv": "^16.4.5",
    "hono": "^4.6.2",
    "ioredis": "^5.4.1",
    "is-uuid": "^1.0.2",
    "openai": "^4.68.0",
    "pg": "^8.13.1",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.5",
    "superstruct": "^2.0.2",
    "tuki_logger": "^0.3.0",
    "tuki_rules": "^0.1.0",
    "uuid": "^11.0.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "jest": "^29.7.0"
  }
}
